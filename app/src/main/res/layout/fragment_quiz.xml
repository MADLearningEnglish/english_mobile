<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/white"
    tools:context=".presentation.feature.study.quiz.QuizFragment">

    <!-- Header -->
    <LinearLayout
        android:id="@+id/layoutHeader"
        android:layout_width="match_parent"
        android:layout_height="56dp"
        android:layout_marginTop="32dp"
        android:paddingHorizontal="16dp"
        android:gravity="center_vertical"
        android:orientation="horizontal"
        app:layout_constraintTop_toTopOf="parent">

        <ImageButton
            android:id="@+id/btnBack"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:src="@drawable/ic_close"
            app:tint="#4A4A68" />

        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:layout_marginStart="16dp"
            android:gravity="center_vertical"
            android:orientation="horizontal">

            <TextView
                android:id="@+id/tvCurrentProgress"
                android:layout_width="32dp"
                android:layout_height="32dp"
                android:background="@drawable/bg_circle_green"
                android:gravity="center"
                android:textColor="@color/white"
                android:textSize="14sp"
                android:textStyle="bold"
                tools:text="2" />

            <ProgressBar
                android:id="@+id/progressBar"
                style="?android:attr/progressBarStyleHorizontal"
                android:layout_width="0dp"
                android:layout_height="8dp"
                android:layout_weight="1"
                android:layout_marginStart="8dp"
                android:layout_marginEnd="8dp"
                android:max="100"
                android:progressDrawable="@drawable/bg_progress_bar_thick"
                tools:progress="30" />

            <TextView
                android:id="@+id/tvTotalScore"
                android:layout_width="32dp"
                android:layout_height="32dp"
                android:background="@drawable/bg_circle_gray"
                android:gravity="center"
                android:textColor="#4A4A68"
                android:textSize="14sp"
                android:textStyle="bold"
                tools:text="60" />

        </LinearLayout>
    </LinearLayout>

    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:fillViewport="true"
        android:overScrollMode="never"
        app:layout_constraintTop_toBottomOf="@id/layoutHeader"
        app:layout_constraintBottom_toBottomOf="parent">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:padding="24dp">

            <!-- Question Prompt (Definition + Pronunciation) -->
            <TextView
                android:id="@+id/tvPrompt"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:textColor="#333333"
                android:textSize="22sp"
                android:textStyle="bold"
                tools:text="/'bæg.ɪdʒ/ - hành lý" />

            <!-- Optional Image preview -->
            <ImageView
                android:id="@+id/imgVisualCue"
                android:layout_width="wrap_content"
                android:layout_height="120dp"
                android:layout_marginTop="16dp"
                android:scaleType="centerInside"
                android:visibility="gone"
                tools:visibility="visible"
                tools:src="@tools:sample/backgrounds/scenic" />

            <!-- "Chọn câu trả lời" label -->
            <TextView
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="32dp"
                android:text="Chọn câu trả lời"
                android:textColor="#4A4A68"
                android:textSize="16sp"
                android:textStyle="bold" />

            <!-- Choices -->
            <LinearLayout
                android:id="@+id/layoutOptions"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="16dp"
                android:orientation="vertical">

                <com.google.android.material.button.MaterialButton
                    android:id="@+id/btnChoice1"
                    style="@style/Widget.Material3.Button.OutlinedButton"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:layout_marginBottom="12dp"
                    android:minHeight="56dp"
                    android:textAlignment="textStart"
                    android:paddingHorizontal="20dp"
                    android:textSize="16sp"
                    android:textColor="#333333"
                    android:backgroundTint="@color/white"
                    app:strokeColor="#E5E7EB"
                    app:strokeWidth="1dp"
                    app:cornerRadius="8dp"
                    android:textAllCaps="false"
                    tools:text="suitcase (n)" />

                <com.google.android.material.button.MaterialButton
                    android:id="@+id/btnChoice2"
                    style="@style/Widget.Material3.Button.OutlinedButton"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:layout_marginBottom="12dp"
                    android:minHeight="56dp"
                    android:textAlignment="textStart"
                    android:paddingHorizontal="20dp"
                    android:textSize="16sp"
                    android:textColor="#333333"
                    android:backgroundTint="@color/white"
                    app:strokeColor="#E5E7EB"
                    app:strokeWidth="1dp"
                    app:cornerRadius="8dp"
                    android:textAllCaps="false"
                    tools:text="baggage (n)" />

                <com.google.android.material.button.MaterialButton
                    android:id="@+id/btnChoice3"
                    style="@style/Widget.Material3.Button.OutlinedButton"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:layout_marginBottom="12dp"
                    android:minHeight="56dp"
                    android:textAlignment="textStart"
                    android:paddingHorizontal="20dp"
                    android:textSize="16sp"
                    android:textColor="#333333"
                    android:backgroundTint="@color/white"
                    app:strokeColor="#E5E7EB"
                    app:strokeWidth="1dp"
                    app:cornerRadius="8dp"
                    android:textAllCaps="false"
                    tools:text="author (n)" />

                <com.google.android.material.button.MaterialButton
                    android:id="@+id/btnChoice4"
                    style="@style/Widget.Material3.Button.OutlinedButton"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:layout_marginBottom="12dp"
                    android:minHeight="56dp"
                    android:textAlignment="textStart"
                    android:paddingHorizontal="20dp"
                    android:textSize="16sp"
                    android:textColor="#333333"
                    android:backgroundTint="@color/white"
                    app:strokeColor="#E5E7EB"
                    app:strokeWidth="1dp"
                    app:cornerRadius="8dp"
                    android:textAllCaps="false"
                    tools:text="service (n)" />

            </LinearLayout>
        </LinearLayout>
    </ScrollView>

    <!-- LOADING OVERLAY -->
    <ProgressBar
        android:id="@+id/progressLoading"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:indeterminateTint="@color/purple_primary"
        android:visibility="gone"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <!-- COMPLETE OVERLAY -->
    <LinearLayout
        android:id="@+id/layoutComplete"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:background="@color/white"
        android:orientation="vertical"
        android:gravity="center"
        android:padding="32dp"
        android:visibility="gone"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <ImageView
            android:layout_width="240dp"
            android:layout_height="240dp"
            android:src="@drawable/img_celebration_penguin"
            android:layout_marginBottom="24dp"
            android:scaleType="centerInside" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Tuyệt vời!"
            android:textColor="#333333"
            android:textSize="32sp"
            android:textStyle="bold"
            android:layout_marginBottom="8dp" />

        <TextView
            android:id="@+id/tvCompleteSubtitle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:gravity="center"
            android:textColor="#666666"
            android:textSize="16sp"
            android:layout_marginBottom="32dp"
            tools:text="Bạn đã hoàn thành bài học 60 từ." />

        <Button
            android:id="@+id/btnCompleteBack"
            style="@style/Widget.Material3.Button"
            android:layout_width="match_parent"
            android:layout_height="56dp"
            android:backgroundTint="#008855"
            android:text="Hoàn tất"
            android:textColor="@color/white"
            android:textSize="16sp"
            android:textStyle="bold"
            app:cornerRadius="16dp" />
    </LinearLayout>

</androidx.constraintlayout.widget.ConstraintLayout>
