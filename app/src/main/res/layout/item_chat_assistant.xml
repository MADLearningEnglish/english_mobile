<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:paddingHorizontal="16dp"
    android:paddingVertical="8dp">

    <TextView
        android:id="@+id/labelAssistant"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="40dp"
        android:letterSpacing="0.06"
        android:text="@string/ai_label_assistant"
        android:textAllCaps="true"
        android:textColor="@color/ai_chip_text_muted"
        android:textSize="10sp"
        android:textStyle="bold"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <com.google.android.material.imageview.ShapeableImageView
        android:id="@+id/avatarAssistant"
        android:layout_width="36dp"
        android:layout_height="36dp"
        android:layout_marginTop="4dp"
        android:scaleType="centerCrop"
        android:src="@drawable/ic_person"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/labelAssistant"
        app:shapeAppearanceOverlay="@style/ShapeAppearanceOverlay.App.CornerSize50Percent"
        app:strokeColor="@color/ai_card_stroke"
        app:strokeWidth="1dp" />

    <FrameLayout
        android:id="@+id/bubbleFrame"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginStart="8dp"
        android:layout_marginEnd="16dp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toEndOf="@id/avatarAssistant"
        app:layout_constraintTop_toTopOf="@id/avatarAssistant">

        <TextView
            android:id="@+id/bubbleAssistant"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="@drawable/bg_chat_assistant_bubble"
            android:lineSpacingExtra="2dp"
            android:paddingHorizontal="14dp"
            android:paddingTop="10dp"
            android:paddingEnd="40dp"
            android:paddingBottom="10dp"
            android:textIsSelectable="true"
            android:textColor="@color/ai_text_on_light"
            android:textSize="15sp"
            tools:text="Hello! Welcome to our cafe." />

        <ImageButton
            android:id="@+id/btnSpeak"
            android:layout_width="34dp"
            android:layout_height="34dp"
            android:layout_gravity="bottom|end"
            android:layout_marginEnd="4dp"
            android:layout_marginBottom="4dp"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:contentDescription="@string/ai_listen"
            android:padding="5dp"
            android:src="@drawable/ic_volume_up"
            app:tint="@color/primary" />
    </FrameLayout>
</androidx.constraintlayout.widget.ConstraintLayout>
