<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:paddingHorizontal="16dp"
    android:paddingVertical="4dp">

    <LinearLayout
        android:id="@+id/correctionCard"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginStart="40dp"
        android:background="@drawable/bg_chat_correction_card"
        android:orientation="vertical"
        android:padding="12dp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:gravity="center_vertical"
            android:orientation="horizontal">

            <ImageView
                android:layout_width="18dp"
                android:layout_height="18dp"
                android:contentDescription="@null"
                android:src="@drawable/ic_edit"
                app:tint="@color/ai_title_navy" />

            <TextView
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_marginStart="6dp"
                android:layout_weight="1"
                android:letterSpacing="0.06"
                android:text="@string/ai_correction_title"
                android:textColor="@color/ai_title_navy"
                android:textSize="11sp"
                android:textStyle="bold" />

            <TextView
                android:id="@+id/linkWhy"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="@string/ai_why_link"
                android:textColor="@color/primary"
                android:textSize="12sp" />
        </LinearLayout>

        <TextView
            android:id="@+id/correctionText"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="8dp"
            android:lineSpacingExtra="2dp"
            android:textColor="@color/ai_text_on_light"
            android:textSize="14sp"
            tools:text="Instead of 'want', try 'would like'." />
    </LinearLayout>
</androidx.constraintlayout.widget.ConstraintLayout>
